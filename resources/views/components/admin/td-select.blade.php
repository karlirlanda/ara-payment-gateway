{{-- Row-select cell — the first <td> of each selectable admin-table row. --}}
<td class="admin-col-select w-px px-3 text-center align-middle">
    <input type="checkbox" class="size-3.5 cursor-pointer accent-[var(--color-accent)]" aria-label="{{ __('Select row') }}" />
</td>
